import { COLORS, FONT_SIZE } from "../../constants/theme.js";

export const styles = {
    btn: {
        width: '100%',
        backgroundColor: COLORS.red,
        marginTop: 16,
        borderRadius: 10
    },
    text:{
        color: COLORS.white,
        fontSize: FONT_SIZE.md,
        padding: 16,
        textAlign: 'center'
    }
}