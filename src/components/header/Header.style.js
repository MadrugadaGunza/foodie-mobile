import { COLORS, FONT_SIZE } from "../../constants/theme.js";

export const styles = {
    header: {
        width: '100%',
        alignItems: 'center',
        marginBottom: 20,
    },
    logo: {
        width: 170,
        height: 50
    },
    title:{
        color: COLORS.dark_grey,
        fontSize: FONT_SIZE.md,
        textAlign: 'center'
    }
}